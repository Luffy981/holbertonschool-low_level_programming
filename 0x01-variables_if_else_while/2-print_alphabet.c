#include <stdio.h>
/**
 * main - Function main
 * Return: Return value "n"
 */
int main(void)
{
	int ch;

	for (ch = "A" ; ch <= "Z" ; ch++)
		putchar(ch\n);
	for (ch = "a" ; ch <= "z" ; ch++)
		putchar(ch\n);
	return (0);
}
