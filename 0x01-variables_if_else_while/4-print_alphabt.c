#include <stdio.h>
/**
 * main - Function main
 * Return: Return value "n"
 */
int main(void)
{
	char i;

	for (i = 'a' ; i <= 'z'; i++)
	{
		for (i != "e")
		{
			putchar(i);
		}
	}
	putchar('\n');
	return (0);
}
